import { BuscarciberPipe } from './buscarciber.pipe';

describe('BuscarciberPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarciberPipe();
    expect(pipe).toBeTruthy();
  });
});
